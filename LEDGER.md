# LEDGER — the wrong-answers log

Every time a skill here is applied and the outcome is worth learning from, it goes in this table —
misfires next to wins. This is the trust artifact: it proves the skills are lived in and corrected,
not frozen advice. It is also the fuel for the learning loop.

## Why a LEDGER (not just a changelog)

Borrowed discipline: record what the tool got WRONG next to what it got right. A practice that keeps
being invoked while only misfiring is worse than one that's honestly retired. **"Last useful outcome,"
not "last invoked," is the death signal** — a skill with no useful outcome across recent applicable
contributions is a candidate for retirement.

Format: date | skill | context (repo/PR) | outcome (worked / misfired / stale / **pruned**) | what changed.

**A skill should get shorter or sharper over time, not just longer.** Every entry here so far records
something ADDED to a skill — a new checklist item, a new Rationalizations row. That is the failure mode
worth naming: *sediment*, where stale wording survives because adding felt safer than deleting, and one
rule ends up stated three ways that slowly drift apart. So a removal is a first-class outcome — logged
as `pruned`, with what came out and what replaced it. When a new rule supersedes old wording, delete the
old wording in the same edit and say so here; a skill that only accretes is decaying, however green its
checks are.

Where an entry concerns whether a skill **routes** (loads when it should), the measured rate lives in the
matching case comment in [`tests/routing-evals.sh`](tests/routing-evals.sh) — one source of truth, since a
rate re-measured later would otherwise drift between two files. Entries here record what changed and why.

Every entry below is the author's own application, which caps what this log can learn: one person's
houses, one person's blind spots. If you ran a skill against a real repo — especially if it misfired,
or never fired at all — the **[I tried it](../../issues/new?template=i-tried-it.yml)** issue template
feeds this table from outside it. A misfire from a house I've never contributed to is worth more here
than another win from one I have.

## Entries

| date | skill | context (repo/PR) | outcome | what changed |
|------|-------|-------------------|---------|--------------|
| 2026-07-07 | obey-the-houses-own-tooling | apache/solr #4612 (SOLR-18302) | misfired → corrected | Hand-created the changelog + GJF-upgrade files instead of running the house task. A Solr committer caught the tool-bypass: *"AI is not using the Gradle tasks we've built & documented (including AGENTS.md)… the file doesn't match the pattern that tool creates."* Recovered by owning it (*"Fair — I did hand-create these rather than run writeChangelog. I'll regenerate them with the task."*) and regenerating with `writeChangelog`. Sharpened the skill's "never hand-craft a generated file" checklist item. |
| 2026-07-20 | consolidate-a-scattered-thread *(navigation/communication; skill proposed, see feedback §B.2)* | apache/solr SOLR-17764 (reply to a Solr committer) | worked *(sourcing-receipt)* | The committer: *"unclear where to further that conversation"* — the graceful-shutdown truth was fragmented across ~8 JIRA tickets + PRs + commits + dev@. Instead of "reply here", traversed the JIRA link graph, read every linked ticket, and handed back a map untangling the conflated concerns into distinct homes (incidental flakiness / test-framework CCE / upstream jetty root cause / SolrJ retry gap). Verifying each claim in code corrected two of my own earlier wrong framings. A worked *communication/navigation* application; logged as a sourcing-receipt, not a merge-receipt. |
| 2026-07-23 | offer-dont-grab | jetty #15472 (fixes #13569, assigned to the HTTP/2 lead) | worked | #13569 was assigned to the HTTP/2 lead but dormant (assignee never commented, no linked PR). Built a TDD-verified fix, then opened the PR with explicit offer framing in the body ("the offered fix, no pressure — feel free to take it a different way") rather than silently grabbing the assigned issue. No grab; PR now in review awaiting the assignee's nod. Honored the guardrail against stacking offers with the same reviewer. |
| 2026-07-23 | verify-before-a-committer-comment | jetty #15435 (fixes #15368) | misfired → corrected | Pushed commit `fe9149e` and — in the same motion — posted a reply citing the tests ("also still passes") before the PR's CI settled. CI then came back red on two UNRELATED flakes: #14901 H3_QUICHE and a jetty-proxy concurrent-load test, neither touched by the change. A "tests pass" claim next to a red CI badge undermines credibility even when the change is innocent. New rule: local green ≠ CI green — wait for the PR's CI, and if a comment asserts test status, confirm CI FIRST (`gh pr checks`). Recovered by posting a follow-up naming the two flakes. |
| 2026-07-25 | use-the-tool-for-its-purpose | apache/solr-orbit #55–#60 | worked *(sourcing-receipt)* | Ran a real N=5 × 3-config benchmark campaign to answer an actual question (Lucene 11 vs 10.4), rather than auditing code for smells. `aggregate` could not run at all: 3 defects, each hidden behind the previous, all in the gap between "it ran" and "you have a result". 3 issues + 3 PRs, one fix each, every finding reproduced against the real method with a passing control. Two candidate findings **withheld** by the same checklist: a `__pycache__` failure that was my own `cp -R` staging (a real `git clone` is clean → downgraded to a `.gitignore` nit), and "the workloads repo is undocumented" (it is documented — `benchmark.ini:28` + 8 doc pages → rescoped to the narrow `--workload-path` parity gap). |
| 2026-07-25 | port-the-report-upstream | opensearch-project/opensearch-benchmark #1093–#1098 | worked *(sourcing-receipt)*, with a corrected guess | Proved the 3 downstream defects **inherited** by diffing both files from `origin/main` (byte-identical apart from 3 import lines, 295 lines each), reproduced them upstream **by running OSB**, and reported all 6 artifacts cross-referencing their downstream counterparts. CI green incl. DCO. Corrects my own earlier written guess that the unconditional blocker "appears Solr-port-specific" — upstream carries the identical wrong attribute; a guess stated in a document meant to be evidence. Re-deriving anchors against the second repo's `origin/main` (prompted by noticing the clone sat on a fix branch) caught **5 stale anchors** before posting: dispatch line 1186 vs 1215, subcommand `run` vs deprecated `execute-test`, issue numbers that autolink per-repo, an `untriaged` label that exists in one house only, DCO required in one house only. |
| 2026-07-25 | verify-before-a-committer-comment | apache/solr-orbit #58 | misfired → caught pre-post | Drafted PR body claimed *"Five new tests… covering … `calculate_rsd` with nulls…"*; the diff had **two** tests and no `calculate_rsd` test. Caught by diffing the branch against `origin/main` before posting — not by a reviewer. Posted body names only the two that exist, states they were verified red by restoring main's `aggregator.py`, and discloses the gap openly. New checklist item: **check the PR body against its own diff, count by count, before posting** — a body written from what you *intended* drifts from what the commit contains, and the reviewer reads the prose beside the diff. |
| 2026-07-26 | one-fix-one-pr-then-coordinate *(new skill, back-filled)* | jetty #15472/#15473 + solr-orbit #58–#60 / OSB #1096–#1098 | worked *(sourcing-receipt)* | A coverage audit against the companion journal found this practice applied **twice** with nothing in the library describing it: the jetty cluster (two fixes split out of one branch of 5 mixed commits, one commit per PR, hub comment on the neutral root issue #15368 while the contested PR's cross-link was held) and the aggregate cluster (6 PRs across two houses, one commit each, numbering kept parallel). Both re-verified via `gh` before the skill shipped — commit counts, the hub comment quoted verbatim. Routes 3/3, then 8/8 with no regression in its neighbors. The gap is instructive: the sourcing skills *generate* clusters, so packaging them was the step the library grew into and never wrote down. |
| 2026-07-26 | verify-before-a-committer-comment *(pruned + sharpened)* | luceneutil #600 | worked → sharpened | Folded the inline-reply lesson in as one checklist item rather than a 17th skill: answer an inline review comment **in its thread**, with the `gh api .../comments/<id>/replies` invocation, not `gh pr comment`. On #600 an inline `src/build.gradle:4` question got a top-level reply — the reviewer read it, approved and merged a minute later, so the answer *worked* while his comment still reads as unanswered on the review. Deliberately not a skill: one command's worth of guidance does not earn its own routing surface, and a thin skill competing with this one would cost more than it adds. |
| 2026-07-26 | port-the-report-upstream *(routing, not application)* | this repo — `tests/routing-evals.sh` | misfired → corrected | The skill was **unroutable**: it shipped with no `Use when…` clause at all, and after one was appended it still fired **0/3** on "this project looks forked from another one — does the original still have the same problem?" (Claude answered directly, never loading it). Two tools found two halves of the same defect: `tools/validate-skills.sh` caught the *missing* clause, then the routing evals showed the appended clause still didn't *route* — a description can satisfy every structural rule and still never fire. Cause: the clause sat in the middle, behind four lines of anchor-re-derivation mechanics, and the trigger terms answered "fork/port" but not the question a person actually asks. Fixed by opening the description with the routing case and adding the phrasings ("does the original have this", "is this fixed upstream", "report it upstream too") → **3/3, then 2/2 in a full-suite run**. Lesson: a receipt is worth nothing if the skill never loads; the `description` is a routing surface first and a summary second. |
| 2026-07-27 | nudge-with-new-information *(new skill)* | apache/solr #4638/#4640/#4643 + lucene #16411 + luceneutil #595/#604 (batch A) vs jetty #15472/#15473 (batch B) | worked *(sourcing-receipt)*, after a partial misfire | Two nudge batches the same morning, four hours apart. **Batch A**: 6 *identical* reminders in 5 minutes — *"Are there any objections to its merger?"* — which passed the whose-turn test but failed the other two. Measured the house rather than guessing: 200 recently-merged `apache/solr` PRs, creation → first non-bot event by someone other than the author = **median 0.33 d, p75 1.05 d, p90 4.80 d, and 63/200 merged with ZERO human comments**. So several nudged PRs were inside p75, and silence is a poor proxy for neglect in this house. Two of the six hit the same maintainer inside a minute; on luceneutil #595 the generic nudge also displaced a *specific* open item — my own unanswered `lastMod` offer. Meanwhile the 3 PRs with **zero** comments (#4644/#4648/#4651, one green-lit on dev@) got none. **Batch B**: 2 comments carrying triage — a failing test **reproduced on pristine `jetty-12.1.x`** (#15473), and for #15472 the *absence* of a reproduction over 18 runs plus a racy 1500 ms-vs-1000 ms assertion, that class's 10 prior flaky issues, and a stale build-1-vs-build-2 observation. Also confirms nudging is house-sanctioned: Solr's guide says *"If no one responds to your patch after a few days, please make friendly reminders."* New skill built from the asymmetry: three tests (their turn → past the house's p90 → carries a fact), never batch, and manufacture the fact when you can. |
| 2026-07-27 | *(harness, not a skill)* `tests/routing-evals.sh` | this repo | misfired → fixed | Adding the new skill appeared to break routing **across every case at once** — `stalled-pr`, `whose-turn` and even the long-stable `red-ci` all returned `saw: (none)`. Not a regression: the Claude CLI refuses to nest (*"Claude Code cannot be launched inside another Claude Code session"*), and the harness swallowed that error, so a failure to *launch* was indistinguishable from a failure to *route*. Found by running the harness's own command by hand. Fixed with `unset CLAUDECODE` plus a comment naming the symptom, so the next person reads "all cases (none) = harness breakage, not routing". After the fix: `stalled-pr` **2/2**, `whose-turn` **2/2** — no collision with its nearest neighbor despite `track-whose-court` already carrying "nudge vs wait" in its description. |
| 2026-07-27 | nudge-with-new-information *(OUTCOME — corrects the skill's own prediction)* | apache/lucene #16411 + luceneutil #595/#604 + jetty #15368 | worked, **better than the skill predicted** | Four hours after the 6 generic Batch-A reminders, **3 of 6 drew substantive maintainer engagement in a 15-minute window (12:03–12:18)**: `apache/lucene#16411` got **its first review ever** (a committer asking for hot-path benchmarks + whether the optimization generalizes to two adjacent cases); `luceneutil#604` a conditional +1 **plus a direct question** (*"+1 to rename the other two cases as long as they are also batch-optimized?"*); `luceneutil#595` the maintainer taking up **the very offer the generic nudge had displaced** (*"+1 for `lastMod` `searchAfter` tasks"*, with a design and a pointer to his own existing Python tool). Separately, jetty `#15368` drew a maintainer acknowledgement 35 min after the two evidence-carrying Batch-B comments: *"the H2 shutdown mechanism is a bit in a sorry state… we want to be certain that they get properly fixed once and for all. Stay tuned."* **So "adds no information" was right about the text and wrong as a prediction of effect** — corrected in place: the description, the Purpose, one Rationalizations row and a new OUTCOME block in the receipt now say the three tests buy **cost control, not effect** (a fact-carrying reminder cannot be resented, misrouted, or burn the single follow-up). Kept the asymmetry that *did* survive: Batch A's replies each hand work back (run benchmarks, verify batch-optimization, find a tool), while Batch B asked for one cheap maintainer action (re-run CI). Both are progress; only one was free. |
| 2026-07-27 | read-the-projects-origin-story | apache/solr-orbit (after #55–#60 were already filed) | worked *(sourcing-receipt)*, but **too late to be free** | Read the 3-thread, 35-message arc that *created* the project — only after filing 12 artifacts against it. Four things I'd been missing: the author had publicly declared the incompleteness I "found" (*"a MVP and WIP… Only one workload / dataset is ported so far"*); the tool won a bake-off **because of** the capability my fixes restored, which raises the claim from "I fixed 3 bugs" to "I restored the mechanism this tool was selected for"; two committers' structural objections sit unresolved inside their own +1 votes (tripwires for any proposal); and the maintainer who invited my proposal had *lost* the original tool argument, while the two people with real standing were in neither my inbox nor my notes. Also: a converter tool + workload guide I was about to propose building **already existed in the repo**. Nothing shipped was wrong — but every framing was weaker than the evidence supported. New skill; the cost of doing this after rather than before is the entry. |
| 2026-07-27 | read-the-projects-origin-story *(routing collision, caught by the evals)* | this repo — `tests/routing-evals.sh` | misfired → corrected | Adding this skill **broke a neighbor**: `fork-upstream` fell from 3/3 to **1/3** immediately after, with the misses answered directly rather than stolen (`saw: (none)`). Cause: my new description opened with *"a young project, **a port**, or a subproject"* and repeated *"is a fork/port"*, so two skills claimed fork/port vocabulary and the router split the difference into neither. Fixed by narrowing this skill to its actual trigger — **about to PROPOSE something** — moving "port" out of the opening clause, and adding an explicit hand-off line in the description (*"For 'does the ORIGINAL of this fork still have the same bug?' use port-the-report-upstream — that is code lineage, this is decision history"*). After the fix, measured across repeated runs: `fork-upstream` went 1/3 → **7/9**, then **3/3 twice more with the full suite at 9/9** on a later re-measure; `why-does-this-exist` **3/3** throughout. Every miss in the 7/9 window was answered directly (`saw: (none)`), never routed to this skill — so the collision itself was gone from the first fix, and the residue was the harness's known direct-answer mode rather than lingering competition. Rates live in the eval file's case comments (single source); `fork-upstream` is the case to watch alongside `post-fix-neighborhood`. Lesson for the next skill: a new description **competes** with the existing ones, so re-run the neighbors' cases, not just the new one — a green new case can hide a regression next door — and re-run enough times to tell a collision (miss routes elsewhere) from noise (miss answers directly). |
| 2026-07-27 | nudge-with-new-information *(new skill, from a misfire of my own)* | apache/solr #4638/#4640/#4643, apache/lucene #16411, luceneutil #595/#604 → jetty #15472/#15473 | **misfired → corrected the same morning** | Posted **6 identical reminders in 4 minutes** (08:02–08:06 UTC), each *"Are there any objections to its merger?"*. Whose-turn held, but the other two tests failed: measured on 200 recently-merged Solr PRs, first human response runs **median 0.33 d, p75 1.06 d, p90 4.83 d, and 63/200 merge with ZERO human comments** — so several nudged PRs were inside p75, and silence in this house is not neglect. One sentence fitting six threads carries no information by construction; two of them hit the same maintainer **17 seconds apart**; and on luceneutil #595 the generic ping *displaced* a better message — my own unanswered offer was the only open item. Meanwhile the three PRs where a nudge was most defensible (#4644/#4648/#4651, **0 comments each**) got none. Corrected by 10:43: two evidence-carrying comments on jetty #15473/#15472 — one naming the failing test and reporting it **reproduces on pristine `jetty-12.1.x` without the change**, the other saying plainly it **does not** reproduce (18 runs) and carrying different facts instead (racy 1500 ms sleep vs 1000 ms idle timeout, 10 prior flaky issues, a stale badge from build 1 vs build 2). Same problem, same morning: a notification vs a triage report. The two comments deliberately differ in verb strength because only one earned the stronger one. Also logged: three threads where holding back was correct (a live architectural reservation, a committed merge window, an already-unanswered request of mine). Every figure re-derived independently before the skill shipped; house quote verified at `dev-docs/how-to-contribute.adoc:78`. |
| 2026-07-27 | state-the-noise-floor *(new skill, back-filled)* | solr-orbit benchmarking + corpus mining — internal, no PR | worked *(sourcing-receipt)* | Two independent applications, both catching my **own** error before a maintainer saw it: N=1 endorsed a −13% `index-append` regression, N=2 endorsed it again against an *estimated* "<±0.2%" floor, and **N=5 retracted it** — the tool's own RSD over five identical runs was **14.33%**, so the estimate was off ~70× and the signal sat inside the floor. The same campaign exposed 14 of 19 operations as rate-pinned by construction and a **79.24% RSD** carrying a meaningless −53.55%. Separately, shuffling closure dates (5 trials, fixed seed) corrected an already-published *"917 stale"* to **917 vs 633 expected by chance = 1.45×** — 69% base rate — and the credibility check was that **narrowing the filter raised the ratio** to 1.74×, which an artefact does not do. The same control **confirmed** a different claim (88% vs shuffle trials of 24/26/27%), which is why it is a discriminator rather than a way to talk yourself down. Every figure re-verified against its source document before the skill shipped. Routes 3/3; full suite 10/10 with no neighbor regression — the check the previous skill's collision taught. |
| 2026-07-27 | read-the-houses-agent-file-first | apache/solr — my own notes, not a PR | **misfired for weeks, caught only by an unrelated question** | I had written, as justification for stripping AI co-author trailers, *"Apache convention is that the human contributor owns the commit (AGENTS.md says the same)."* **Neither clause was true.** `AGENTS.md:3` *points onward* — *"Also see `dev-docs/how-to-contribute.adoc` for some guidelines when using genAI"* — and that document asks the **opposite** of silence: *"For major AI-assisted contributions, disclose the use of AI tools in your PR description."* Practice went further still: **12 commits on `main` in ~18 months carry `Co-authored-by: <AI model>`, from 3 committers** (38 from 6 authors if Copilot-class trailers are counted too), including the author of the AI policy. The foundation's own guidance recommends a different token (`Generated-by:`) that appears **0** times in the repo. So I had collapsed three distinct facts — foundation recommendation, house policy, committer practice — into one invented convention matching none of them, and the invented rule was **stricter** than reality (the bias to expect: conservatism feels safe and is still wrong). The underlying contributor preference was legitimate and unchanged; the failure was laundering a preference into house authority with no citation. Sharpened the skill with 3 checklist items (follow the file's pointers; never attribute without quote+path; measure written-vs-observed) + 4 rationalizations + a counter-receipt. |

## Reconstruction cost (sourcing skills)

The sourcing skills (`follow-the-thread`, `consolidate-a-scattered-thread`,
`use-the-tool-for-its-purpose`, `port-the-report-upstream`) don't just
succeed-or-misfire — each firing costs a measurable amount of *by-hand* cross-channel work
(graph hops, artifacts re-read, trackers crossed) that is redone from scratch every invocation,
with no memory between runs. Logging that cost turns "it worked once" into a running measure of
how much reconstruction the manual method is actually being asked to do.

Columns: date | skill | hops | artifacts re-read | trackers crossed | note.

| date | skill | hops | artifacts | trackers crossed | note |
|------|-------|------|-----------|------------------|------|
| 2026-07-20 | consolidate-a-scattered-thread | ~8 (full link-graph walk) | ~8 JIRA tickets + linked PRs + commits + dev@ posts | 3 (ASF JIRA, GitHub, dev@) | rebuilt the SOLR-17764 map from scratch by hand; the traversal + read alone "took hours" (felt-pain note). No cached graph — a second run would redo all of it. |
| 2026-07-23 | follow-the-thread | 4 (symptom → upstream → subsystem → adjacent) | SOLR-17764 flake + jetty #15368/#15435/#13569/#15472/#13602/#15473 | 2 (Solr JIRA, Jetty GitHub) | one downstream CI flake sourced 3 upstream issues + 3 PRs; each hop re-ran `gh` search + subsystem read live, discarded after. |
| 2026-07-25 | use-the-tool-for-its-purpose | 3 (blocker → next blocker → parity gap; each only visible once the prior was fixed) | 15 real benchmark runs (~38 min each) + `aggregator.py` + `metrics.py` + `benchmark.py` + `benchmark.ini` + 8 doc pages (the "is it documented" check) | 1 (GitHub) + local runs | the cost here is **wall-clock, not hops**: ~9.5 h of benchmark runtime is what bought the findings, and it is unavoidable — the defects are only reachable at realistic N. Two candidate findings discarded after re-testing (own-setup check, documented-already check). |
| 2026-07-25 | port-the-report-upstream | 2 (downstream site → upstream site) × 5 anchor classes re-derived | both `aggregator.py` from `origin/main` (diffed), `benchmark.py` dispatch, `metrics.py` assignment + legacy keys, 4 upstream PR numbers verified to resolve, 2 label sets, 2 CONTRIBUTING/DCO contracts | 2 (Apache GitHub, OpenSearch GitHub) | re-derivation is the whole cost and it is **not** compressible by copying: 5 anchors were stale, and the clone being on a fix branch (not `main`) is what exposed them. A cached "same code" answer would have shipped all 5. |

`tools/check-status.sh` now logs its own version of this automatically — one line per run
(`ts | channels | items | ball_on_you_github | ball_on_you_cross_tracker`) to
`~/.contrib-receipts/status-log.tsv`, so the cross-channel sweep's cost accrues without manual
bookkeeping. The `cross_tracker` count is the one that matters: a reply owed on a channel a
GitHub-only view can't see.

The counts answer *how much*; they can't answer *which thread, and what decided it* — which is
what an entry above actually needs, written weeks later. So the same run also appends one JSON
record per item to `~/.contrib-receipts/decisions.jsonl`
(`ts | channel | item | verdict | last | note`), e.g.
`{"channel":"asf-jira","item":"SOLR-3284","verdict":"you-cross-tracker","note":"invisible to a
GitHub-only sweep"}`. A row in this table is then assembled from records rather than recalled —
the same discipline the skills demand of a PR body. Both files live outside the repo: the
records carry real maintainer names and are private working notes, never publishable.

## Notes

- Every entry above is a real, dated application sourced from the contribution journal (companion
  agentic-oss notes: `lessons/confirm-pr-ci-green-before-posting.md`, `solr/issues/SOLR-17764/
  consolidation.md`, `SCOREBOARD.md`, and per-reviewer profile notes). Nothing here is invented;
  an application that can't be verified against the source is omitted, not guessed.
- The SOLR-17764 row is a **sourcing/communication-receipt** (a method that demonstrably worked before
  any PR merged), not a merge-receipt — see the two receipt classes in the README.
- The two 2026-07-25 sourcing rows are likewise **sourcing-receipts**: 6 issues + 6 PRs open across two
  projects, none merged yet. The upstream CI green on #1096/#1097/#1098 is *observed* (OpenSearch runs
  checks for outside contributors immediately); solr-orbit's #58/#59/#60 sit at `action_required`
  pending a maintainer, so the green claimed in those PR bodies is the local suite and is stated as such.
  Verified via `gh` 2026-07-25. Companion agentic-oss notes:
  `lessons/use-the-tool-for-its-purpose-to-find-bugs.md`, `lessons/mocks-cannot-verify-attribute-names.md`,
  `solr/issues/solr-orbit-aggregate/index.md`, `solr/candidates/osb-aggregate-upstream.md`.
